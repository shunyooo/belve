.DS_Store
node_modules/
.build/
.swiftpm/
.claude/
archived/
Belve.app/
test-results/
tests/ui-baselines/

# Swift build artifacts (stray)
*.o
*.d
*.swiftdeps

# Stale resource dir from v1 era
WebMarkdown/
GhosttyKit.xcframework/

# Legacy (VS Code fork remnants)
.vscode/
extensions/
out/
src/
dist/
tmp/

# Character sprite workflow (sandbox) — intermediate artifacts only
sandbox/character-sprites/source/
sandbox/character-sprites/variants/
sandbox/character-sprites/reports/
sandbox/character-sprites/debug/
sandbox/character-sprites/.backup-*/

# Adjacent clones of other repos (e.g. dropped here for cross-referencing).
ghostty/

# Local-only files (secrets, agent state, session handoff notes) — never commit.
.env
.haltr/
HANDOFF.md

# Python bytecode cache (py_compile 等の副産物)
__pycache__/
*.pyc
